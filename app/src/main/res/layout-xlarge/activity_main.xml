<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    android:background="#DE8181">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="New Order"
        android:id="@+id/NewOrderBtn"
        android:textSize="30dp"
        android:background="#0df2ef"
        android:textStyle="normal|bold|italic"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentStart="true"
        android:padding="50dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Find Order"
        android:id="@+id/FindOrderBtn"
        android:textSize="30dp"
        android:background="#10ff93"
        android:textStyle="normal|bold|italic"
        android:padding="50dp"
        android:layout_centerVertical="true"
        android:layout_alignEnd="@+id/NewOrderBtn"
        android:layout_alignStart="@+id/NewOrderBtn" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Exit"
        android:id="@+id/exitBtn"
        android:textSize="20dp"
        android:background="#ee3c15"
        android:textStyle="normal|bold|italic"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:padding="10dp"
        android:layout_alignBottom="@+id/UpdProdBtn"
        android:layout_alignEnd="@+id/FindOrderBtn"
        android:layout_alignTop="@+id/UpdProdBtn" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Update Products"
        android:id="@+id/UpdProdBtn"
        android:textSize="20dp"
        android:textStyle="normal|bold|italic"
        android:padding="25dp"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:layout_below="@+id/FindOrderBtn"
        android:layout_alignParentStart="true"
        android:layout_marginTop="53dp"
        android:background="#ffd81b"
        android:layout_toStartOf="@+id/exitBtn" />

</RelativeLayout>
